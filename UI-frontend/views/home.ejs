<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css
" 
    integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <div class="container">
        <div class="jumbotron">
            <h1>Hello <%= username %></h1>
            <form action="/create" method="post">
                <div class="row">
                    <div class="col-md-6">
                        <input type="hidden" name="userid" value = "<%= userid %>">
                        <div>Name: <input type="text" name="name" required></div>
                    </div>
                    <div class="col-md-6">
                        <div>Price: <input type="text" name="price" required></div>
                    </div>
                </div>
                <br>
                <button class="btn btn-primary" type="submit">Add Product</button>
            </form>
            <br>
            <table>
                <tr>
                    <th>Productid</th>
                    <th>Userid</th>
                    <th>Name</th>
                    <th>Price</th>
                    <th>Update</th>
                    <th>Delete</th>
                </tr>
                <% products.forEach(element => { %>
                    <tr>
                        <td><%= element.productid %> </td>
                        <td><%=  element.userid %></td>
                        <td><%=  element.name %></td>
                        <td><%=  element.price %></td>
                        <td><a href="/update/<%= element.productid %>">Update</a></td>
                        <td><form action="/delete/<%= element.productid %>" method="post"><button type="button" class="btn btn-danger" type="submit">Delete</button></form></td>
                    </tr>
                <% }) %>
            </table>
            <br>
            <form action="/logout" method="post">
                <button class="btn btn-outline-primary float_right" type="submit">Logout</button>
            </form>
        </div>
    </div>
</body>
</html>